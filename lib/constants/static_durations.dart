part of 'constants.dart';

class StaticDurations {
  static const Duration pageTransition = Duration(milliseconds: 250);

  static const Duration bottomSheetLoading = Duration(milliseconds: 300);

  static const Duration splashScreenLoading = Duration(seconds: 4);
  static const Duration bottomSheetHide = Duration(seconds: 3);

}
